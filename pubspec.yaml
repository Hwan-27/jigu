name: jigu
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # SDK 버전 범위 수정

dependencies:
  flutter:
    sdk: flutter
  share:
  fluttertoast: ^8.2.1
  image_picker: ^0.8.7+5
  get: ^4.6.5
  firebase_core: ^2.5.0 # firebase_core의 최신 버전 번호로 수정
  firebase_auth: ^4.10.0
  cupertino_icons: ^1.0.2
  google_sign_in: ^6.1.5
  cloud_firestore: ^4.9.2 
  http: ^0.13.3
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - images/
